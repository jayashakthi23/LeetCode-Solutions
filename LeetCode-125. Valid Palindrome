class Solution:
    def isPalindrome(self, s: str) -> bool:
        n=s.lower()
        r=""
        for i in n:
            if i.isalpha() or i.isdigit():
                r+=i
        if r==r[::-1]:
            return True
        else:
            return False
