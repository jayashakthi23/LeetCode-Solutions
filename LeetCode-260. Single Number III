class Solution(object):
    def singleNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        r=[]
        for i in nums:
            if nums.count(i)==1:
                r.append(i)
        return r
        
