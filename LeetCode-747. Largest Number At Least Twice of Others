class Solution(object):
    def dominantIndex(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        r=max(nums)
        for i in nums:
            if i*2>r and i!=r:
                return -1
                break
        return nums.index(r)
