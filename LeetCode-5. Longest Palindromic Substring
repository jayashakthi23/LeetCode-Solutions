class Solution:
    def longestPalindrome(self, s: str) -> str:
        l=""
        for i in range(len(s)):
            for j in range(i,len(s)):
                sub=s[i:j+1]
                if sub==sub[::-1]:
                    if len(l)<len(sub):
                        l=sub
        return l
        
