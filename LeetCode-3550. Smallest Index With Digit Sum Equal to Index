class Solution(object):
    def smallestIndex(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
       
        for i in range(len(nums)):
            a=sum(int(d) for d in str(nums[i]))
            if i==a:
                return i
                break
        return -1
